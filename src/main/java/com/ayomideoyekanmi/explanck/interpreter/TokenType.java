package com.ayomideoyekanmi.explanck.interpreter;

/**
 * @author: aoyekanmi
 * @date: 11/03/2022
 */
public enum TokenType {
    // +, -, /, x, >, >=, <, <=, ^, (,)
    LEFT_PAREN, RIGHT_PAREN,
    STAR, SLASH,
    PLUS, MINUS,
    POWER,

    NUMBER
}
